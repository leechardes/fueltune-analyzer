# Production Dependencies for FuelTune Streamlit
# Optimized for production deployment

# Core requirements (production versions)
-r requirements.txt

# Production Web Server
gunicorn>=21.2.0           # WSGI server for production
uvicorn[standard]>=0.24.0  # ASGI server (if using async features)

# Database Production
psycopg2-binary>=2.9.7     # PostgreSQL adapter (production database)

# Caching Production
redis>=5.0.0               # Redis for production caching
hiredis>=2.2.3             # High-performance Redis client

# Monitoring and Logging
prometheus-client>=0.18.0  # Prometheus metrics
sentry-sdk>=1.38.0         # Error tracking
structlog>=23.2.0          # Structured logging
python-json-logger>=2.0.7  # JSON logging

# Performance and Optimization
orjson>=3.9.0              # Fast JSON processing
msgpack>=1.0.7             # Efficient serialization

# Security Production
cryptography>=41.0.0       # Cryptographic operations
bcrypt>=4.1.0              # Password hashing
PyJWT>=2.8.0               # JSON Web Tokens

# HTTP and Network
requests[security]>=2.31.0 # HTTP with security features
certifi>=2023.7.22         # SSL certificates

# Configuration Management
pydantic-settings>=2.0.0   # Settings management
python-decouple>=3.8       # Environment configuration

# Background Tasks (Optional)
celery>=5.3.0              # Distributed task queue
kombu>=5.3.0               # Messaging library

# Data Processing Optimization
numba>=0.58.0              # JIT compilation for NumPy
bottleneck>=1.3.7          # Fast NumPy array operations

# System Integration
psutil>=5.9.0              # System information

# Health Checks
healthcheck>=2.0.0         # Health check utilities

# Time and Date Handling
python-dateutil>=2.8.2     # Date utilities

# Production Utilities
click>=8.1.7               # Command line interface